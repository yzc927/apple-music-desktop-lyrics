# Apple Music 桌面歌词详细使用说明

## 1. 软件简介

Apple Music Desktop Lyrics 是一个面向 Windows 的桌面歌词伴侣。它不会修改 Apple Music，
而是在独立的透明悬浮窗中显示当前歌词、下一句和行内演唱进度。窗口可以置顶、锁定、穿透鼠标，
适合在聊天、写作、浏览网页或游戏时继续查看歌词。

当前仓库同时保留 `macos/` 目录，方便以后加入 macOS 原生实现；现阶段可运行版本为 Windows 版。

## 2. 主要功能

- 优先使用 LRCLIB 的同步歌词，找不到时自动回退到 Apple Music 自带歌词。
- 显示当前句和下一句，并用颜色表现当前句的演唱进度。
- 支持窗口置顶、位置锁定和歌词区域鼠标穿透。
- 解锁时可以拖动窗口、调整大小和关闭到系统托盘。
- 调整窗口大小时，歌词字号会按比例同步变化。
- 支持每次提前或延后 0.5 秒，并按歌曲分别记忆偏移量。
- 支持精选字体、预设颜色和按歌手自动配色。
- 自动保存窗口位置、窗口大小、字体、颜色、置顶与锁定等设置。
- 关闭窗口默认收进右下角系统托盘；只有托盘右键菜单中的“退出”才会结束程序。

## 3. 系统要求

- Windows 10 19041 或更高版本，推荐 Windows 11。
- Windows 版 Apple Music。
- 默认使用 LRCLIB 同步歌词，需要能够访问互联网。
- 使用自包含发布版时不需要另外安装 .NET；从源码运行时需要对应的 .NET SDK。

## 4. 安装与启动

正式发布程序位于：

```text
windows/publish/AppleMusicDesktopLyrics.exe
```

双击 EXE 即可启动。建议先打开 Apple Music 并开始播放歌曲，再启动桌面歌词。程序启动后会在
系统托盘创建图标；即使悬浮歌词窗口暂时没有显示，也可以从托盘右键菜单打开管理界面。

如果桌面快捷方式已经创建，它应指向上述发布程序。更新程序时保持该路径不变，快捷方式不需要重建。

## 5. 日常使用

### 5.1 未锁定状态

未锁定时可以：

- 按住歌词区域拖动整个窗口。
- 从窗口边缘或角落调整大小。
- 使用工具栏调整歌词快慢、置顶、颜色和自动配色。
- 点击关闭按钮，把窗口收进系统托盘。

窗口放大或缩小时，当前句和下一句的字号会一起按比例变化。

### 5.2 锁定状态

锁定后，歌词窗口主体会变成鼠标穿透区域。点击歌词后面的聊天框、按钮或网页控件时，事件会直接
传递给后面的应用，不会被透明歌词窗口挡住。

锁定状态下其他工具按钮和关闭按钮全部隐藏，只保留解锁入口。解锁图标平时也不显示；鼠标移动到
歌词区域并停留约 1 秒后，它会淡入，移出区域后淡出。这样既能避免误触，也不会长期遮挡画面。

### 5.3 置顶

置顶功能用于让歌词保持在普通窗口上方。锁定与置顶是两个独立状态：可以置顶但不锁定，也可以
置顶并锁定穿透。

### 5.4 关闭与退出

点击歌词窗口的关闭按钮只会隐藏窗口并保留托盘程序。需要再次显示或管理时，右键单击托盘图标。

要完全结束程序，请使用托盘右键菜单中的“退出”。这样可以避免误点关闭后丢失正在使用的歌词状态。

## 6. 托盘管理界面

管理界面只能从系统托盘右键菜单打开，歌词窗口本身不提供入口，以降低锁定或游戏时误触的概率。

管理界面可用于：

- 显示或隐藏歌词窗口。
- 切换置顶和锁定状态。
- 选择歌词字体。
- 选择手动颜色或启用按歌手自动配色。
- 查看和调整当前歌曲的时间偏移。
- 将偏移恢复为 0 秒。
- 退出程序。

## 7. 字体选择

默认字体为 `Microsoft YaHei UI`，也就是当前版本原本使用的字体。管理界面会从精选列表中筛选
本机实际安装的字体，避免选择后出现系统替代字体。

精选列表包括：

- Microsoft YaHei UI
- Microsoft YaHei
- DengXian
- Segoe UI
- Yu Gothic UI
- Meiryo
- Source Han Sans SC
- Noto Sans CJK SC
- LXGW WenKai

字体选择立即应用于当前句、进度填色层和下一句，并在下次启动时恢复。

## 8. 颜色与自动配色

手动模式提供少量经过筛选的清晰颜色，避免复杂取色器造成视觉负担。自动模式会优先查找歌手的
专属配色；没有预设的歌手会根据名称生成稳定颜色，同一歌手每次得到相同结果。

合作歌曲会先识别 `&`、`feat.`、`with`、`×`、顿号和逗号等分隔方式，再组合最多三位歌手的
代表色。正式组合拥有自己的整体配色时，会优先使用组合配色，不会错误拆成成员名称。

Liyuu 当前使用清透的天蓝渐变，主色为偏蓝色，辅色略亮。其他已收录歌手也使用各自稳定的精选配色。

## 9. 歌词来源与同步方式

程序按以下顺序获取歌词：

1. LRCLIB 同步歌词。
2. Apple Music 自带歌词。
3. 两者都不可用时显示“未找到同步歌词”。

### 9.1 LRCLIB 同步歌词

程序会先使用歌名、歌手、专辑和歌曲时长向 LRCLIB 查找同步 LRC。LRCLIB 提供明确的逐行时间戳，
便于应用手动时间偏移和正确处理伴奏空档，因此作为默认来源。第三方曲库可能缺歌，也可能匹配到现场版、
伴奏版或不同长度的版本；无结果时程序会回退到 Apple Music。

### 9.2 Apple Music 官方后备歌词

Windows 版 Apple Music 没有公开稳定的桌面歌词 API。程序通过 Windows UI Automation 读取
Apple Music 已经显示在歌词面板中的公开文本元素，并在需要时自动打开歌词面板。

部分 Apple Music 版本会直接提供 `CurrentLine`；较新的版本可能只提供虚拟化的 `Line` 列表。
程序同时兼容两种结构。对于后者，程序根据歌词滚动面板中稳定的高亮位置判断当前句和下一句。
Apple Music 显示三个点的前奏或间奏会被识别为伴奏状态：悬浮窗显示 `•••`，不提前给下一句填色，
直到 Apple Music 切入正式歌词。

官方歌词模式的换句时机来自 Apple Music。由于 Apple 没有公开逐字或逐音节时间戳，当前句内部的
颜色填充仍是平滑估算，不能保证与每一个字的发音完全一致。

## 10. 歌词快慢调整

工具栏中的“歌词慢”和“歌词快”按钮每次调整 0.5 秒，图标旁会用较小的 `0.5` 提示调整幅度。

- 歌词偏慢：让歌词提前 0.5 秒。
- 歌词偏快：让歌词延后 0.5 秒。
- 偏移归零：从托盘管理菜单将当前歌曲恢复为 0 秒。

偏移量按歌曲分别保存。切换歌曲后会加载该歌曲自己的设置，不会把上一首歌的修正错误应用到下一首。

Apple 官方模式能够修正行内进度显示，但官方当前行何时切换仍以 Apple Music 为准；LRCLIB 模式则可
直接移动整条 LRC 时间轴。

## 11. 设置保存位置

用户设置保存在：

```text
%LOCALAPPDATA%\AppleMusicDesktopLyrics\settings.json
```

逐歌曲偏移保存在同一应用数据目录。程序会保存：

- 窗口左上角位置。
- 窗口宽度和高度。
- 字体。
- 手动颜色和自动配色状态。
- 置顶和锁定状态。
- 每首歌曲的时间偏移。

如果显示器数量、缩放比例或桌面分辨率发生变化，程序会尽量将恢复后的窗口限制在可见工作区内，
避免窗口完全落到屏幕外。

## 12. 常见问题

### Apple Music 明明有歌词，桌面却显示未找到

先确认 Apple Music 的歌词面板能够正常打开。程序会自动尝试打开它，但 Apple Music 更新后可能改变
UI Automation 元素。重新启动 Apple Music 和桌面歌词通常可以恢复；如果仍失败，请提供 Apple Music
歌词面板和桌面歌词同时出现的截图。

### 歌词停在某一句不动

确认歌曲没有暂停。如果 Apple Music 正在播放而官方歌词仍在移动，说明读取器可能没有跟上新版界面
的滚动结构。此时记录歌曲名、当前 Apple 歌词和桌面歌词显示内容，有助于校准兼容逻辑。

### 歌词会突然跳句或回退

LRCLIB 可能匹配到不同版本，系统媒体进度也可能出现很小的回拨。程序已经过滤短暂回退；个别歌曲仍可
通过每次 0.5 秒的偏移进行修正。LRCLIB 无结果时才使用 Apple 官方后备歌词。

### 锁定后仍点不到后面的窗口

锁定后只有淡入的解锁图标应接收鼠标，歌词主体必须穿透。如果问题只在某个屏幕缩放比例或多显示器
布局中出现，请记录显示器缩放比例和歌词窗口所在屏幕。

### 重启后位置或大小没有恢复

检查 `%LOCALAPPDATA%\AppleMusicDesktopLyrics\settings.json` 是否能够写入。移动或调整大小后稍等片刻，
再从托盘菜单退出程序，可确保最后状态已经保存。

## 13. 隐私说明

程序不会读取或保存 Apple ID、密码、Apple Music Cookie、令牌或私人资料。Apple 官方歌词来自
Windows 无障碍接口中已经显示的文本。

为了搜索默认同步歌词，歌名、歌手、专辑和歌曲时长会发送给 LRCLIB。
窗口设置、字体、颜色和逐歌曲偏移只保存在本机。

## 14. 从源码构建

在仓库根目录运行：

```powershell
dotnet build .\windows\AppleMusicDesktopLyrics.csproj -c Release
dotnet publish .\windows\AppleMusicDesktopLyrics.csproj -c Release -o .\windows\publish
```

发布结果为单文件 Windows x64 程序。构建产生的 `bin`、`obj`、`publish` 和临时更新目录不会提交到 Git。

## 15. 其他播放器与 macOS 版本

悬浮窗、托盘管理、字体、颜色、窗口状态、逐歌曲偏移和歌词渲染均可复用。要支持 QQ 音乐、Spotify、
网易云音乐等播放器，主要需要为每个播放器实现两个适配层：

1. 当前曲目与播放进度读取。
2. 播放器自带歌词读取。

如果播放器支持 Windows 系统媒体会话，第一部分通常较容易复用；第二部分取决于播放器是否公开歌词
接口，或是否通过 UI Automation 暴露当前歌词。各播放器都可以先使用 LRCLIB，同步歌词无结果时再
尝试播放器自身歌词。

macOS 原生预览版已经放在 `macos/`：使用 AppKit/SwiftUI 实现悬浮窗、菜单栏、置顶穿透和管理界面，
通过 Music AppleScript 读取播放信息，通过 AXUIElement 读取官方后备歌词。它尚未在 Mac 环境编译，
首次实机测试、权限配置和调试步骤请参阅 `macos/README.md`。
