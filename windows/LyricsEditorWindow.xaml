<Window x:Class="AppleMusicDesktopLyrics.LyricsEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="本地 LRC 编辑器" Width="760" Height="620" MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterOwner" Background="#FFF5F6F8">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="本地 LRC 编辑器" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="每行格式：[00:12.34] 歌词。保存后将永久优先于网络歌词。"
                       Margin="0,5,0,12" Foreground="#FF6F7684" />
        </StackPanel>
        <TextBox x:Name="Editor" Grid.Row="1" AcceptsReturn="True" AcceptsTab="True"
                 FontFamily="Consolas" FontSize="15" Padding="12" TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 TextChanged="Editor_TextChanged" />
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <TextBlock x:Name="ValidationText" VerticalAlignment="Center" Foreground="#FF6F7684" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Width="84" Margin="0,0,8,0" Padding="10,6" Click="Cancel_Click" />
                <Button Content="保存本地歌词" Width="126" Padding="10,6" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
